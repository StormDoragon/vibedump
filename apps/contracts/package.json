{
  "name": "@vibedump/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy": "anchor deploy"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.0"
  }
}
