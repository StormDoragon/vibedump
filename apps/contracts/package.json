{
  "name": "@vibedump/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "if command -v anchor >/dev/null 2>&1; then anchor build; else echo 'Anchor CLI not found — skipping contract build'; fi",
    "test": "if command -v anchor >/dev/null 2>&1; then anchor test; else echo 'Anchor CLI not found — skipping contract tests'; fi",
    "deploy": "anchor deploy"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.0"
  }
}
